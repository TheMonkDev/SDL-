#ifndef DEFAULT_H
#define DEFAULT_H

#include <SDL.h>

extern const char* DEF_TITLE;
const int DEF_POS = SDL_WINDOWPOS_CENTERED;
const int DEF_WIDTH = 800;
const int DEF_HEIGHT = 600;
const int DEF_MODE = SDL_WINDOW_RESIZABLE | SDL_WINDOW_SHOWN;

#endif
